server:
  port: 8087
spring:
  application:
    name: ai-service
  datasource:
    url: jdbc:mysql://localhost:3306/AI_db?createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

# 👇 CẤU HÌNH GEMINI (Đã sửa lại chuẩn)
gemini:
  api-key: AIzaSyDW8-VSAJI53_lzl4lVwB1Esf6gb-NMPOI
  # URL chuẩn cho model 1.5 Flash (Nhanh và Rẻ hơn), bỏ đoạn ?key= đi để code tự xử lý
  url: https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-latest:generateContent?key=
