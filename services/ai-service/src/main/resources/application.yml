server:
  port: 8084 
spring:
  application:
    name: ai-service
  datasource:
    url: jdbc:mysql://localhost:3307/collab_ai_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: "root" # Nhớ kiểm tra lại pass của bạn
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# 👇 CẤU HÌNH GEMINI (Đã sửa lại chuẩn)
gemini:
  api-key: AIzaSyDW8-VSAJI53_lzl4lVwB1Esf6gb-NMPOI
  # URL chuẩn cho model 1.5 Flash (Nhanh và Rẻ hơn), bỏ đoạn ?key= đi để code tự xử lý
  url: https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-latest:generateContent?key=