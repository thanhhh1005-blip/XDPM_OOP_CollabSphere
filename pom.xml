<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
        https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.3</version>
        <relativePath/>
    </parent>

    <groupId>com.collabsphere</groupId>
    <artifactId>collab-sphere</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>collab-sphere</name>
    <description>CollabSphere Microservices Project</description>
    <modules>
        <module>api-gateway</module>
        <module>services/auth-service</module>
        <module>services/ai-service</module>
        <module>services/workspace-service</module>
        <module>services/communication-service</module>
        <module>services/evaluation-service</module>
        <module>services/resource-service</module>
        <module>services/collaboration-service</module>
        <module>services/class-service</module>
        <module>services/subject-service</module>
        <module>services/notification-service</module>
        <module>services/team-service</module>
        <module>services/project-service</module>
        <!-- Sau này thêm service nào thì điền thêm vào đây -->
    </modules>

    <properties>
        <java.version>21</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring.boot.version>3.2.3</spring.boot.version>
        <spring-cloud.version>2023.0.0</spring-cloud.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <!-- Thêm/Sửa plugin này để bật chế độ lưu tên tham số -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version> <!-- Hoặc version khác -->
                <configuration>
                    <parameters>true</parameters> <!-- 👈 QUAN TRỌNG NHẤT -->
                </configuration>
            </plugin>
            
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <parameters>true</parameters> 
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>