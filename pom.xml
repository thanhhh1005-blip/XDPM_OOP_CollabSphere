<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" 
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.collab</groupId>
    <artifactId>collab-sphere</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging> <!-- QUAN TRỌNG: Đánh dấu đây là dự án cha -->
    
    <name>collab-sphere</name>
    <description>CollabSphere Microservices Project</description>

    <modules>
        <!-- Khai báo các module con em đang có -->
        <module>shared</module>
        <module>api-gateway</module>
        <!-- Các service nghiệp vụ -->
        <module>services/workspace-service</module>
        <module>services/communication-service</module>
        <!-- Sau này thêm service nào thì điền thêm vào đây -->
    </modules>

    <properties>
        <java.version>21</java.version>
        
        <spring.boot.version>3.2.3</spring.boot.version>
        <spring.cloud.version>2023.0.0</spring.cloud.version>
    </properties>
    
    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring.cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Lombok dùng chung cho tất cả -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>
</project>